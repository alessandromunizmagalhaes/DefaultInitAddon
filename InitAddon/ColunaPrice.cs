namespace InitAddon
{
    public class ColunaPrice : Coluna
    {
        public ColunaPrice(string nome, string descricao, bool obrigatorio = false) : base(nome, descricao, obrigatorio)
        {
        }
    }
}
