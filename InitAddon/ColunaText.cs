namespace InitAddon
{
    public class ColunaText : Coluna
    {
        public ColunaText(string nome, string descricao, bool obrigatorio = false) : base(nome, descricao, obrigatorio)
        {
        }
    }
}
